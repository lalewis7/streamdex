var util = require('../util.js');
var config = require('../config.json');

module.exports = {

    getSnapshots(link_id){},

    getCurrentShapshot(link_id){},

    insertSnapshot(link_id, timestamp, ver, data){},

    updateSnapshot(link_id, timestamp, ver, data){},

    deleteSnapshot(link_id, timestamp, ver, data){}

}